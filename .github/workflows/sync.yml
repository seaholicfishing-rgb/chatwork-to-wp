name: chatwork-to-wp

on:
  schedule:
    # JST 6:00〜23:59 の間、5分ごとに実行（UTCに換算: 21-23時台 + 0-14時台）。
    # 公開リポジトリ＝Actions無料無制限なので短間隔でOK。
    # ※GitHub Actionsのscheduleは数分の遅延がよくあるため、反映は数分〜十数分かかる場合があります。
    - cron: '*/5 0-14,21-23 * * *'
  workflow_dispatch:        # 「今すぐ反映したい」時に手動実行できる

permissions:
  contents: write           # state.json をリポジトリに書き戻すため

concurrency:
  group: cw-to-wp
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run sync
        env:
          CHATWORK_TOKEN: ${{ secrets.CHATWORK_TOKEN }}
          WP_USER: ${{ secrets.WP_USER }}
          WP_APP_PASSWORD: ${{ secrets.WP_APP_PASSWORD }}
        run: python sync.py

      - name: Commit state
        run: |
          if [ -n "$(git status --porcelain state.json)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add state.json
            git commit -m "update state [skip ci]"
            git push
          else
            echo "state に変更なし"
          fi
